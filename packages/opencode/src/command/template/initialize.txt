Create or update the root `AGENTS.md` for this repository.

The result must be compact, project-specific guidance that helps future OpenCode sessions avoid mistakes. Include a line only when an agent would likely miss it without investigation.

User-provided focus or constraints:
$ARGUMENTS

## 1. Investigate

Read the highest-value executable sources of truth before writing:
- existing instruction files, `README*`, root manifests, workspace configuration, and lockfiles
- build, test, lint, format, typecheck, code generation, CI, and task-runner configuration
- a small number of representative entrypoints when configuration does not reveal package boundaries or runtime flow

Trace the real implementation path before describing architecture. Search for existing helpers and established patterns first, then consider the standard library, native platform capabilities, and installed dependencies before assuming custom code is required. Prefer executable configuration and scripts when prose is stale or contradictory.

Record only facts verified from this repository. Keep directory-scoped rules in their existing nested instruction files; do not promote them to the root unless they truly apply repository-wide. Ask the user one short batch of questions only when an important convention cannot be established from the repository.

## 2. Write

Only create or update `${path}/AGENTS.md`. Do not modify any other project file or add, install, copy, or configure skills.

Preserve useful verified guidance already present, remove stale or duplicated claims, and organize the result into short sections and bullets. Prioritize:
- exact developer commands, focused test commands, required command order, and working-directory requirements
- package boundaries, real entrypoints, generated files, migrations, environment loading, and operational constraints
- repository-specific style, testing, review, branch, release, and code-generation rules that differ from common defaults

Preserve commands, paths, symbols, URLs, environment variables, and versions exactly. Remove filler, repetition, hedging, tutorials, exhaustive file trees, generic software advice, and claims that could not be verified. A simple repository should produce a simple file; a large repository should still include only facts that change how an agent works.

Do not copy tool, skill, framework, or persona names into `AGENTS.md` unless they are independently verified project facts.

## 3. Verify

Re-read `${path}/AGENTS.md` and verify that:
- every instruction is supported by a repository source or an explicit user constraint
- commands, paths, symbols, URLs, environment variables, and versions match their sources character-for-character
- root guidance does not claim that nested-only rules apply everywhere
- no duplicated, generic, speculative, or stale guidance remains
- no file other than `${path}/AGENTS.md` was changed

If any check fails, correct `AGENTS.md` before finishing.
