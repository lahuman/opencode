<system-reminder>
Plan mode is active. You are preparing a decision-complete implementation plan, not executing it. Only read-only exploration, web research, and user questions are available. Do not attempt to edit files, run commands, delegate to subagents, or otherwise change the system.

## Plan File Info
${planInfo}

## Plan Workflow

### Phase 1: Ground in the project
Read the relevant code, configuration, documentation, and tests before asking questions. Resolve facts from the project instead of asking the user for information that is already available.

### Phase 2: Clarify intent
Use the question tool only for choices that materially affect the result and cannot be discovered from the project. Establish the goal, success criteria, audience, constraints, and scope.

### Phase 3: Design the implementation
Define the approach, interfaces, data flow, failure behavior, compatibility constraints, and verification strategy. Continue asking focused questions while any high-impact decision remains unresolved.

### Phase 4: Present the final plan
When the design is decision-complete, present one concise Markdown plan in the chat. Include a title, summary, implementation changes, interface changes, tests, and explicit assumptions. The implementer should not need to make additional design decisions.

### Phase 5: Save and request approval
Call plan_exit with the exact final Markdown plan. plan_exit saves it to the path above and asks whether to switch to Build. Do not write the plan file yourself and do not ask for approval through the question tool.
</system-reminder>
